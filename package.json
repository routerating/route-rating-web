{
  "name": "route-rating-web",
  "private": true,
  "license": "MIT",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "bump": "HUSKY_BYPASS=true lerna version --no-git-tag-version --no-push",
    "postbump": "git add . && git commit -m 'chore: bump version'",
    "clean": "lerna run clean",
    "format:markdown": "yarn lint:markdown --fix",
    "format": "lerna run format && yarn format:markdown",
    "lint:markdown": " markdownlint '**/README.md' --ignore '**/node_modules/**' --ignore '**/CHANGELOG.md'",
    "lint": "lerna run lint && yarn lint:markdown",
    "setup": "yarn && yarn bootstrap && yarn build && ./packages/scripts/dist/generateWorkspace.js",
    "test": "lerna run test"
  },
  "devDependencies": {
    "@commitlint/cli": "^9.0.1",
    "@commitlint/config-lerna-scopes": "^9.0.1",
    "@lukeshay/eslint-config-typescript": "^1.0.4",
    "@lukeshay/rollup-config": "^0.2.0",
    "@types/jest": "^26.0.3",
    "@typescript-eslint/eslint-plugin": "^3.4.0",
    "cross-env": "^7.0.2",
    "eslint": "^7.3.1",
    "eslint-plugin-jest": "^23.17.1",
    "husky": "^4.2.5",
    "jest": "^26.1.0",
    "jest-junit": "^11.0.1",
    "lerna": "^3.22.1",
    "markdownlint-cli": "^0.23.1",
    "npm-check-updates": "^7.0.1",
    "rollup": "^2.18.1",
    "typescript": "^3.9.5"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "husky": {
    "hooks": {
      "pre-stage": "yarn format",
      "commit-msg": "[[ -n $HUSKY_BYPASS ]] || commitlint -E HUSKY_GIT_PARAMS"
    }
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-lerna-scopes"
    ],
    "rules": {
      "type-enum": [
        2,
        "always",
        [
          "build",
          "chore",
          "ci",
          "docs",
          "feat",
          "fix",
          "refactor",
          "revert",
          "style",
          "test"
        ]
      ]
    }
  },
  "version": "0.0.0"
}
